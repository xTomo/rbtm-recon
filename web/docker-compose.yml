version: '2.4'  # work with docker-compose 1.25.4
services:
  test:
    runtime: nvidia
    image: "nvidia/cuda:12.1.0-devel-ubuntu18.04"
    command: "nvidia-smi"
  reconstructor:
    runtime: nvidia
    build: rbtmrecon/
    volumes:
      - /diskmnt/fast/robotom:/fast
      - /diskmnt/a/makov/robotom/:/storage
      - /home/robotom/rbtm_data/rbtm_storage/data/experiments:/exp_src:ro
#    restart: unless-stopped
    external_links:
      - rbtmstorage_server_1
    # network_mode: bridge
    networks:
      - default
      - rbtmstorage_default
    container_name: web_reconstructor_1
  reconstructor-jupyter:
    runtime: nvidia
    build: rbtmrecon/
    command: bash -c ". /opt/conda/etc/profile.d/conda.sh && conda activate xrecon && umask 0002 && cd /rbtm/recon/ && jupyter lab"
#    runtime: nvidia
    volumes:
      - /diskmnt/fast/robotom:/fast
      - /diskmnt/a/makov/robotom/:/storage
      - /home/robotom/rbtm_data/rbtm_storage/data/experiments:/exp_src:ro
#    restart: unless-stopped
    external_links:
      - rbtmstorage_server_1
    # network_mode: bridge
    networks:
      - default
      - rbtmstorage_default
    ports:
      - "5551:5551"
    container_name: web_reconstructor_jupyter_1
    # Увеличиваем /dev/shm для multiprocessing.shared_memory (multi-GPU реконструкция).
    # Для фантома 1000^3: синограмма ~0.8 GB + объём ~4 GB → нужно ≥ 6 GB.
    # Альтернатива: ipc: host  (использует всю RAM хоста, менее изолированно)
    shm_size: '6gb'
  web:
    build: rbtmwebrecon/
    depends_on:
      - database
    volumes:
      - /diskmnt/a/makov/robotom/:/webrbtm/webrecon/static/tomo_data
#    restart: unless-stopped
    external_links:
      - rbtmstorage_server_1
    # network_mode: bridge
    networks:
      - default
      - rbtmstorage_default
    ports:
      - "5550:5550"
    container_name: web_web_1
  database:
    image: mongo:4.0
    ports:
       - "27017:27017"
    # network_mode: bridge
    container_name: web_database_1
    volumes:
      - ./data/db:/data/db
#    restart: unless-stopped

networks:
  rbtmstorage_default:
    external: true
